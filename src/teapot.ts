/**
 * A random function of randomness.
 *
 * @returns {string} A random string about a tea pot named Jerry.
 */
export default function imaTeaPot(who: string): string {
  return `${who} is a little tea pot.`;
}
